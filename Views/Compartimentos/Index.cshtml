@model IEnumerable<GuardaVolume.Models.Compartimento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tamanho)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disponivel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trancado)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.UsuarioId)
            </th>*@
            @*<th>
                @Html.DisplayNameFor(model => model.Armario)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Numero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tamanho)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disponivel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trancado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UsuarioId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Armario.ArmarioId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompartimentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompartimentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompartimentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
